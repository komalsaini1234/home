import 'package:flutter/material.dart';

const hotel = Icon(Icons.hotel);
const restaurant = Icon(Icons.restaurant);
const train = Icon(Icons.train);
const flight = Icon(Icons.flight);
const bus = Icon(Icons.bus_alert);
